{
  "name": "sianfintech-frontend",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo 'Build complete. Deploy dist/ folder to your web server'"
  }
}